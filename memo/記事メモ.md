Run ./mvnw clean package
/home/runner/work/_temp/635a4931-b8d1-4144-adc7-d16de14757b7.sh: line 1: ./mvnw: Permission denied
Error: Process completed with exit code 126.

→権限無。

git update-index --chmod=+x backend/mvnw
git commit -m "fix: make mvnw executable"
git push

→権限つける

git ls-files --stage | grep mvnw

→確認


Dev containerの設定値は上書きできる

```json
{
  "name": "springboot-backend",
  "dockerComposeFile": [
    "../../docker-compose.yml",
    "../../docker-compose-dev-container.yml"
  ],
  "service": "backend",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "vmware.vscode-boot-dev-pack"
      ]
    }
  }
}
```

```yml
services:
  frontend:
    command: sleep infinity

  backend:
    command: sleep infinity
```

```yml
services:
  frontend:
    image: node:20
    container_name: frontend-cicd-demo
    volumes:
      - ./frontend:/workspace
    working_dir: /workspace/app
    command: sh -c "npm run dev"
    ports:
      - "3000:3000"

  backend:
    image: maven:3.9-eclipse-temurin-17
    container_name: backend-cicd-cdmo
    volumes:
      - ./backend:/workspace
    working_dir: /workspace/demo
    command: mvn spring-boot:run
    ports:
      - "8080:8080"
```

# 確認
```bash
docker compose -f docker-compose.yml -f docker-compose-dev-container.yml config
```